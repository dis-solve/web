# dissolve-web
Front website.

Usage
-
Run a dev instance - with conditional dependencies install and virtualenv activation
```
./run
```

Or just run flask
```
flask run --reload
```

More options
```
./start --help
```
